#include <stdio.h>

int setbits(int x, int p, int n, int y){
    return (x <
}

/*
* Write a function setbits(x, o, n, y) that returns x with the n bits that begin at position p set to the rightmost n bits of y 
* leaving the rest of the bits unchanged.
*/
int main(){
}
